[project]
name = "pdf-forge"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "dotenv>=0.9.9",
    "pypdf>=6.14.2",
]

[dependency-groups]
dev = [
    "mypy>=2.3.0",
]
